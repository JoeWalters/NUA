{
  "name": "nuaserver",
  "version": "2.2.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "node scripts/startup-validator.js",
    "start": "node app.js",
    "start:dev": "npm run prestart && nodemon app.js",
    "dev": "nodemon app.js",
    "db": "npx prisma generate && npx prisma migrate dev",
    "db:deploy": "npx prisma generate && npx prisma migrate deploy",
    "db:reset": "npx prisma migrate reset --force",
    "health": "curl -f http://localhost:4323/health || exit 1",
    "validate": "node scripts/startup-validator.js",
    "logs": "tail -f config/server_logs/app.log"
  },
  "keywords": [],
  "author": "",
  "license": "LGPL-3.0-or-later",
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "cron-validator": "^1.3.1",
    "express": "^4.18.2",
    "node-cron": "^3.0.3",
    "node-schedule": "^2.1.1",
    "node-unifi": "^2.5.1",
    "nodemon": "^3.0.2",
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "prisma": "^5.8.0-dev.42"
  }
}
